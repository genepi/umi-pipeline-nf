/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. 
 */

params {

    help = false
	version = false
	debug = true
	
	input = "umi-pipeline-nf/data/fastq_pass/"
	output = "umi-pipeline-nf_test-run"
	reference = "umi-pipeline-nf/data/ref/lpa-ref2645.fasta"
	reference_fai = "umi-pipeline-nf/data/ref/lpa-ref2645.fasta.fai"
	bed = "umi-pipeline-nf/data/ref/lpa-ref2645.bed"

	subsampling = false
	write_reports = true
	output_format = "fastq"
	filter_strategy_clusters = "quality"
	call_variants = true
	variant_caller = "freebayes"
	
	medaka_model = "r941_min_sup_g507"
}
// NEXTFLOW REPORTING # this defines pipeline metadata	
if(params.output != null){
	dag {
		enabled = true
		file = "${params.output}/nextflow_stats/dag.mmd"
	}
	report {
		enabled = true
		file = "${params.output}/nextflow_stats/report.html"
	}
	timeline {
		enabled = true
		file = "${params.output}/nextflow_stats/timeline.html"
	}
	trace {
		enabled = true
		file = "${params.output}/nextflow_stats/trace.txt"
	}
}
